syntax = "proto3";

import "sync.proto";

package proto.slave;

message Heartbeat {}

message SlaveMessage {
  oneof slave_message {
    Heartbeat heartbeat = 1;
    proto.sync.SyncMessage sync_message = 2;
  }
}
