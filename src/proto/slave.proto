syntax = "proto3";

import "sync.proto";

package proto.slave;

message Heartbeat {}

message SlaveMessage {
  oneof slave_message {
    Heartbeat heartbeat = 1;
    proto.sync.SyncRequest sync_request = 2;
    proto.sync.SyncResponse sync_response = 3;
  }
}
