syntax = "proto3";

import "common.proto";

package proto.master;

message NewKeySpaceSelected {
  repeated proto.common.KeySpaceRange new_ranges = 1;
  uint32 generation = 2;
}

message MasterMessage {
  oneof master_message {
    NewKeySpaceSelected key_space_selected = 1;
  }
}
